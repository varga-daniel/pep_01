SET(TARGET_NAME leszamlal)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/lib)
LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})
AUX_SOURCE_DIRECTORY(. SRC)
ADD_EXECUTABLE(${TARGET_NAME} ${SRC})
TARGET_LINK_LIBRARIES(${TARGET_NAME} lib)
