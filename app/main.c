#include <stdio.h>
#include <stdlib.h>
#include <omp.h>

#include <stopper.h>


int main(int argc, char const *argv[])
{
    stopperOMP sw;
    
    //startSOMP(&sw);
    //stopSOMP(&sw);   

    //tprintfOMP(&sw, " ");
    return 0;
}